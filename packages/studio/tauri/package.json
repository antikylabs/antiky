{
  "name": "@antiky/studio-tauri",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:ghostty": "node scripts/prepare-ghostty.mjs",
    "prepare:node-runtime": "node scripts/node-runtime/prepare.mjs",
    "prepare:project-service": "npm run build:project-service --workspace @antiky/studio-app",
    "prepare:resources": "npm run prepare:project-service && npm run prepare:node-runtime",
    "tauri": "tauri",
    "predev": "npm run prepare:ghostty && npm run prepare:resources",
    "dev": "tauri dev",
    "prebuild": "npm run prepare:ghostty && npm run prepare:resources",
    "build": "tauri build",
    "pretest": "npm run prepare:ghostty && npm run prepare:resources",
    "test": "node --test tests/*.test.mjs && cargo test",
    "pretypecheck": "npm run prepare:ghostty && npm run prepare:resources",
    "typecheck": "cargo check"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
