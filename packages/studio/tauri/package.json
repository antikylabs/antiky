{
  "name": "@antiky/studio-tauri",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:ghostty": "node scripts/prepare-ghostty.mjs",
    "predev": "npm run prepare:ghostty",
    "dev": "tauri dev",
    "prebuild": "npm run prepare:ghostty",
    "build": "tauri build --bundles app",
    "pretest": "npm run prepare:ghostty",
    "test": "node --test tests/*.test.mjs && cargo test",
    "pretypecheck": "npm run prepare:ghostty",
    "typecheck": "cargo check"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
