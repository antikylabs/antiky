/* Generated by BroMetal. Do not edit — recompile with `npx brometal dev`. */
import type { CompiledShader } from 'brometal';

const bloomBlurShader: CompiledShader<{ aPosition: 'vec3' }, Record<string, never>, { uSource: 'sampler2D'; uDirection: 'vec2' }> = {
  wgslSrc: `struct BmUniforms {
  uDirection : vec2f,
}
@group(0) @binding(0) var<uniform> bm_u : BmUniforms;
@group(0) @binding(1) var uSource : texture_2d<f32>;
@group(0) @binding(2) var uSource_sampler : sampler;
struct BmVSIn {
  @location(0) aPosition : vec3f,
}
struct BmVSOut {
  @builtin(position) bm_position : vec4f,
  @location(0) vUv : vec2f,
}
@vertex
fn vs_main(bm_in : BmVSIn) -> BmVSOut {
  var bm_out : BmVSOut;
  bm_out.vUv = ((vec4f(bm_in.aPosition.x, bm_in.aPosition.y, 0.0, 1.0)).xy / (vec4f(bm_in.aPosition.x, bm_in.aPosition.y, 0.0, 1.0)).w * vec2f(0.5, -0.5) + vec2f(0.5));
  bm_out.bm_position = vec4f(bm_in.aPosition.x, bm_in.aPosition.y, 0.0, 1.0);
  bm_out.bm_position.z = (bm_out.bm_position.z + bm_out.bm_position.w) * 0.5;
  return bm_out;
}
@fragment
fn fs_main(bm_in : BmVSOut) -> @location(0) vec4f {
  let step1 = vec2f(bm_u.uDirection.x, bm_u.uDirection.y);
  let centre = textureSample(uSource, uSource_sampler, bm_in.vUv).xyz * 0.1964825501511404;
  let near = (textureSample(uSource, uSource_sampler, bm_in.vUv + step1).xyz + textureSample(uSource, uSource_sampler, bm_in.vUv - step1).xyz) * 0.2969069646728344;
  let mid = (textureSample(uSource, uSource_sampler, bm_in.vUv + step1 * 2.0).xyz + textureSample(uSource, uSource_sampler, bm_in.vUv - step1 * 2.0).xyz) * 0.09447039785044732;
  let far = (textureSample(uSource, uSource_sampler, bm_in.vUv + step1 * 3.0).xyz + textureSample(uSource, uSource_sampler, bm_in.vUv - step1 * 3.0).xyz) * 0.010381362401148057;
  return vec4f(centre + near + mid + far, 1.0);
}
`,
  attributes: { aPosition: 'vec3' },
  instanceAttributes: {},
  uniforms: { uSource: 'sampler2D', uDirection: 'vec2' },
  layout: {"attributes":[{"name":"aPosition","type":"vec3","location":0,"size":3,"divisor":0}],"uniforms":[{"name":"uSource","type":"sampler2D","kind":"1i","size":1,"unit":0,"textureBinding":1,"samplerBinding":2},{"name":"uDirection","type":"vec2","kind":"2fv","size":2,"offset":0}],"uniformBlockSize":16},

};

export default bloomBlurShader;
