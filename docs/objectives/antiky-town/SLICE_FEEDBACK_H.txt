# Feedback, one item per line...
# When done fixing them mark them with "x |" at the start of a line.

---
x | I expected `antiky dev` command to launch a focused dev environment for the demo it was configured for. Why did it launch the website? We were not trying to instrument the website, we want a single page launch canvas full size of game configured sizing/aspect ratio. I expected brometal-town to just come up but instead it launched the whole site.
x | (Already gave you this feedback, but leaving here just incase you messed up again.) I expected `antiky dev` to launch both the demo and the MCP, and any services etc that need to be live for an agent or studio to connect to.
x | (Already gave you this feedback, but leaving here just incase you messed up again.) MCP server is cool, but uses lots of resources. Should it actually do that? I have not seen real users and popular MCP servers make use of resources, and not every MCP client and harness out there works with them correctly. Most of the time everything is just tool calls.
x | (Already gave you this feedback, but leaving here just incase you messed up again.) User facing docs are not written well to a developer, game dev, indie dev, etc coming in wanting to use the framework. They shouldn't mention slices etc and should just be documentation for end users coming to use the cli, framework, studio, etc... It should be written in preparation for a doc-site to be launched eventually. So lets reframe and make sure to include a AGENTS.md and CLAUDE.md that sets the right tone so agents that use that folder and write to those docs do it correctly.
x | Make sure MCP tools have well written descriptions on them. The descriptions should tell an agent how to use the tool they are calling and why it is there.
x | make sure to structure the cli src folder correctly its awefully flat atm... so I'd put things like src/host, src/mcp/server.ts|src/mpc/tools.ts|src/mcp... etc make sure its easy to understand and approachable. We will want the same for framework to be broken down to correct organized structure as we build or the system will become unmaintainable with tons of files in a flat structure.
