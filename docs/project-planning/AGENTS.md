This is not an objective to be used with objective skills.

This is for manual high level project tracking not an individual objective.

Todos under @todos/* are purposefully simple and concise easy oneliners with simple `+tags` and `@references` following todotxt conventions.

Personal convention:

- Nothing = not started
- `\` = in progress
- `x` = completed

The main `todo.txt` is purposefully ordered by priority and under sections for each release planned.
